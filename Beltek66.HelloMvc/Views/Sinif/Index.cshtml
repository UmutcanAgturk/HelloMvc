@model IEnumerable<Beltek66.HelloMvc.Models.Sinif>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sinifad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sinifad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Sinifid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Sinifid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Sinifid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
